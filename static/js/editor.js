const editor=document.getElementById('contentEditor'); const status=document.getElementById('saveStatus'); if(editor){let timer;editor.addEventListener('input',()=>{status.textContent='Unsaved changes';clearTimeout(timer);timer=setTimeout(()=>status.textContent='Ready to save',800)});document.querySelector('[data-command="bold"]')?.addEventListener('click',()=>wrap('**'));document.querySelector('[data-command="italic"]')?.addEventListener('click',()=>wrap('*'));document.getElementById('insertHeading')?.addEventListener('click',()=>{editor.value+='\n## New Section\n'});function wrap(mark){const s=editor.selectionStart,e=editor.selectionEnd;editor.setRangeText(mark+editor.value.slice(s,e)+mark,s,e,'end')}}